ekf_select: # select which source to disable (0) or enable (1)
    override_ekf_config: True   # override default config in ekf_config.yaml
    odom:
      default_odom: 1   # it should be vesc_odom
      rf2o: 0